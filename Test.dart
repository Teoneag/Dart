import 'dart:io';

main() {
  var s =
      'Nici o companie care va solicita serviciile platite ale companiei nu va fi refuzata pe baza unor circumstanye sociale sau culturale.';
  for (int i = s.length - 1; i >= 0; i--) stdout.write(s[i]);
}
